env: # Environment variables to pass to all tasks                   CIRRUS_CLONE_DEPTH: 1 # No need to clone with the whole his>
    CIRRUS_WORKING_DIR: "/tmp/ci" # Where to clone the aosp-bui>
                                                                    # For rclone with google drive or any cloud! After first co>
    # ENCRYPTED variables is unique for every repository, so yo>
    rclone_config: "

task:
    name: aosp # Project name
    timeout_in: 120m # Otherwise it will timeout at 60m automat>
    container:
      image: apon77/aosp:latest # It has built-in setup environ>
      cpu: 8  #8 or less cpu for dummy build, 8 cpu for the fin>      memory: 32G # So that we can have ram when needed, 32G hi>
    download_ccache_background_script: # To download ccache in >      - ./download_ccache # Ccache download script
